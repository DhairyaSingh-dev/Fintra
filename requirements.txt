# ==================================================
# Core Application Dependencies (Lightweight)
# ==================================================
# Install these first for faster builds

Flask>=3.0.0
Flask-SQLAlchemy>=3.1.1
Flask-Cors>=4.0.0
gunicorn>=22.0.0
python-dotenv>=1.0.0
requests>=2.32.0
PyJWT>=2.8.0
yfinance>=0.2.40
curl_cffi>=0.7.0
google-genai>=1.0.0
google-auth>=2.22.0
groq>=0.4.0
psycopg2-binary>=2.9.9
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=14.0.0,<23.0.0
redis>=5.0.0
upstash-redis>=1.0.0
flask-limiter>=3.5.0
flask-socketio>=5.3.0
redisvl>=0.1.0
# Lightweight embedding model - only ~10MB, no PyTorch needed
# Falls back to sentence-transformers or disabled if not available
fastembed>=0.3.0

# Heavy ML dependency - commented out due to Render build timeout
# sentence-transformers requires PyTorch (~2GB) and causes 20+ minute builds
# FastEmbed is preferred, but this is fallback option
# sentence-transformers>=2.5.0

# ==================================================
# Testing Dependencies
# ==================================================
pytest>=8.0.0
pytest-flask>=1.3.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
pytest-asyncio>=0.23.0
faker>=24.0.0
